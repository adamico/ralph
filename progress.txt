02-scaffold: Added bin/ralph (verbatim copy of ~/.local/bin/ralph), MIT LICENSE, and install.sh that drops the script into $PREFIX/bin (default ~/.local/bin). Smoke-tested ralph usage output and bash syntax of both scripts.
03-docs: Added docs/ADR-0001-priority-rules.md documenting the issue header schema (Status/Blocked-by/Blocked-reason), the pick algorithm, COMPLETE/BLOCKED halt conditions, and success/failure commit protocols. Added docs/examples/.ralph.conf with annotated CLAUDE_CMD/SANDBOX_NAME/TEST_CMD/LINT_CMD/STUCK_CPU_SECS defaults. Tests script not present yet (issue 04); docs reviewed by hand.
04-tests: Added tests/pick_next_issue_spec.sh — plain bash, no bats. Sources bin/ralph (which short-circuits when sourced) to exercise pick_next_issue() across 9 fixture scenarios: lone ready, lowest-filename tiebreak, done-skipping, undone vs done Blocked-by deps, multi-dep with one undone, all-blocked → ALL_BLOCKED=1, all-done → empty pick, and 'blocked' status skipped. Captures stdout via tempfile so the function's side-effect ALL_BLOCKED survives (command substitution would fork a subshell). 14/14 assertions pass.
05-github-backend: Added GitHub Issues backend to bin/ralph. New config knobs BACKEND (fs|github, default fs), GH_MILESTONE, GH_REPO, GH_LABEL_READY (status:ready), GH_LABEL_BLOCKED (status:blocked). pick_next_issue_gh() lists open issues in milestone via `gh issue list --json number,labels,body`, filters by ready label, sorts by issue number, parses each body for `Blocked-by: #NN, #MM` and treats a dep as done iff `gh issue view` reports state=CLOSED; sets ALL_BLOCKED=1 when readies exist but every one has an undone dep. build_prompt_gh() instructs the agent to gh-view the issue, run tests/lint, then either close + commit progress on success or stash + add blocked label + comment Blocked-reason on failure. iterate_once now dispatches on $BACKEND: github path emits COMPLETE when no open issues remain in milestone and BLOCKED when ALL_BLOCKED=1, with rc 20/21 unchanged so `ralph run N` halts correctly. Sanity: existing fs-backend tests still 14/14 pass; bash -n clean.
06-readme: Wrote README.md covering install (./install.sh, PREFIX override), fs + github quickstarts, header schema, subcommand table (once/run/status/kill with exit codes), common + GitHub-only config override tables, halt code legend (0/20/21/10), and test invocation. Cross-links ADR-0001 for the pick algorithm. Tests still 14/14.
07-proto-haiku-tick: Implemented trivial tick logger for proto-haiku; appends ISO timestamp line to .scratch/proto-haiku/tick.log on each run. Tests verified (14/14 pass). Ready to observe promise marker emission across multiple runs.
08-gh-projectcards-fix: Fixed GraphQL projectCards deprecation error in gh issue read flow. Updated bin/ralph build_prompt_gh() to instruct agents to use explicit `--json number,title,body,state,labels,milestone,comments` field selection when reading issues via `gh issue view`, bypassing the deprecated projectCards fetch. Updated docs/agents/issue-tracker.md read-issue guidance to match. Tests still pass (14/14).

09	issue-8-optional-milestone: Made milestone arg optional in `ralph once` and `ralph run <N>`. Added `infer_milestone_gh()` which fetches open milestones via `gh api`: exits with error if none exist, uses the single milestone silently if exactly one, shows a numbered interactive picker when stdin is a TTY and multiple exist, or errors with a candidate list in non-TTY contexts (cron, backgrounded). Updated `iterate_once` to call the inference function when no milestone argument is provided; explicit arg still overrides. Amended ADR-0002 consequence #1 to replace "cannot be omitted" with the new inference rules. Tests still 14/14.
10	issue-9-auto-close-milestone: Added close_milestone_gh() to bin/ralph. When iterate_once reaches the COMPLETE state (no open issues left) under the GitHub backend, it resolves the milestone number via `gh api repos/:owner/:repo/milestones --jq` then PATCHes it to state=closed. Called in both the early COMPLETE path (queue already empty before agent runs) and the late COMPLETE path (detected in agent output). Failure to close logs a warning and returns 0 — does not abort the run. ADR-0002 rationale amended to state that auto-close is now implemented. Tests still 14/14.
11	issue-10-to-issues-gh: Created ~/.claude/skills/to-issues-gh/SKILL.md — a global personal skill that converts a PRD/spec into GitHub issues inside a named milestone. The skill: (1) infers a milestone slug from the source material and prompts once to confirm, (2) creates the milestone via `gh api ... POST` if it does not exist, (3) publishes vertical-slice issues in dependency order with Blocked-by references, ready-for-agent label, and --milestone flag so ralph can pick them up with zero extra arguments. Edge cases covered: milestone creation failure, mid-batch issue failure, existing issues in milestone, missing gh CLI. Tests still 14/14.
12	issue-12-stream-json-logs: Switched non-interactive iterate_once() to invoke claude with --output-format stream-json. Output is piped through grep --line-buffered to extract JSON lines, then teed to .ralph-logs/<scope>/iter-<N>-<ts>.jsonl while processing with jq to print clean assistant text to terminal. Promise detection now parses the JSONL log file via jq final_result filter instead of stdout grep, making it robust to tool-call noise. Interactive mode (-i) remains unchanged. Created .gitignore with .ralph-logs/ entry. Implementation works with both CLAUDE_CMD="claude" and CLAUDE_CMD="sbx run claude --". Tests still 14/14.
13	issue-13-session-marker: Added .ralph-logs/<scope>/running session marker file creation and cleanup in non-interactive iterate_once(). When a session starts, a file containing the current shell PID ($$) is written to .ralph-logs/<scope>/running; a trap EXIT is set to remove the file on exit (COMPLETE, BLOCKED, error, or normal end). This allows `ralph inspect` to detect live sessions. Trap ensures cleanup happens even on interrupt (Ctrl+C). Added tests to pick_next_issue_spec.sh to verify file creation and cleanup. All tests pass (16/16 assertions). Bash syntax verified. Ready for `ralph inspect` to use this marker.
14	issue-14-ralph-inspect: Implemented `ralph inspect [<milestone> [<N>]]` subcommand with two modes: list (default) shows a table of all logged iterations with iteration number, timestamp, promise result (COMPLETE/BLOCKED/empty), and parsed issue number; replay mode takes a milestone and iteration N and pipes the JSONL through a jq filter to print clean assistant text with a header. Milestone inference uses the same logic as iterate_once (inferred for GitHub backend, read from MARKER_FILE for fs backend). Helper functions extract_issue_from_jsonl() and extract_promise_from_jsonl() parse the JSONL metadata. Graceful error messages when no logs exist. All acceptance criteria met and tested with realistic JSONL payloads across both GitHub and filesystem backends. Tests still pass (16/16 assertions).
15	issue-15-ralph-inspect-tail: Implemented live-tail mode for `ralph inspect` that activates when a running session is detected (PID file exists in .ralph-logs/<scope>/running). Tailing streams new JSONL lines from the current iteration(s), pipes through jq to extract clean assistant text, prints iteration boundary markers with issue numbers, and prints summary lines with promise results when iterations complete. Per-iteration tracking prevents duplicate header/promise output across polling cycles. Exits cleanly when the session ends (running file deleted). All acceptance criteria met. Tests still pass (16/16 assertions).
16	issue-16-tests: Added comprehensive tests to pick_next_issue_spec.sh covering JSONL log creation, promise detection, and inspect list mode output. Tests use a mock claude command emitting canned stream-json without invoking the real LLM, and follow the plain-bash pattern with fixtures and assert_eq (no bats). Verifies that iterate_once creates .ralph-logs/<scope>/iter-N-*.jsonl with expected content, that COMPLETE/BLOCKED promises are detected from JSONL (not stdout), and that `ralph inspect` tabular output correctly displays iteration number, timestamp, issue, and promise. All fixtures cleaned up after tests. Test count increased from 16 to 24 assertions, all pass.
18	34	record_baseline + read_baseline: Implemented iteration baseline recording for crash recovery. Added record_baseline() function that writes .ralph-logs/<scope>/baseline with HEAD SHA, issue identifier, branch name, and unix timestamp (overwritten each iteration so only latest matters). Added read_baseline() function that parses the baseline file back into BASELINE_SHA, BASELINE_ISSUE, BASELINE_BRANCH, BASELINE_TIMESTAMP global variables; returns 0 on success, 1 if file missing. Both functions are documented inline with stable on-disk key=value format. Integrated record_baseline() into iterate_once() GitHub backend path, called after issue pick but before Claude invocation so baseline captures true pre-iteration state. Created tests/baseline_spec.sh with round-trip test in temp git repo, overwrite test, and missing-file test (8 assertions pass). All syntax checks clean. Updated .ralph.conf LINT_CMD to include new test file. Tests still pass (pick_next_issue_spec.sh: 29/29, baseline_spec.sh: 8/8).
19	35	recovery_plan: pure command-list emitter for interrupted-iteration redo: Implemented recovery_plan() function that takes (baseline_sha, milestone, issue, branch) and returns ordered list of commands to redo interrupted iteration from scratch: git reset --hard <sha>, git clean -fdx, git push --force-with-lease origin <branch>, ralph run 1 <milestone>. Pure function: no git invocation, no network, no filesystem writes. Uses branch argument verbatim (never outputs 'main'). Created tests/recovery_plan_spec.sh with 12 assertions covering output format, branch verbatim usage, command order, and purity (all pass). All syntax checks clean. Updated .ralph.conf LINT_CMD to include new test file. Tests pass (pick_next_issue_spec.sh: 29/29, baseline_spec.sh: 8/8, recovery_plan_spec.sh: 12/12).
20	36	ralph status: flag interrupted iterations (dead PID + baseline): Updated cmd_status() to detect and display interrupted iterations when .ralph-logs/<scope>/running contains a dead PID AND .ralph-logs/<scope>/baseline exists. Reads baseline via read_baseline() to extract issue number and branch, displays in formatted output under "INTERRUPTED iterations" header. Stays silent (no header) when PID is alive or baseline missing. Created tests/cmd_status_spec.sh with 6 assertions covering dead-PID+baseline (flagged), alive-PID (quiet), no-baseline (quiet), and no-running-file (quiet) scenarios. All syntax checks clean. Updated .ralph.conf LINT_CMD to include new test file. Tests pass (pick_next_issue_spec.sh: 29/29, baseline_spec.sh: 8/8, recovery_plan_spec.sh: 12/12, cmd_status_spec.sh: 6/6).
21	37	ralph recover: print-only recovery runbook from baseline: Implemented cmd_recover() subcommand that reads baseline from .ralph-logs/<scope>/baseline, validates issue is still open/ready (warns otherwise), and prints ordered recovery commands filled in with real values. Never executes git push --force-with-lease (print-only, human-triggered). Added recover to usage() text and subcommand case dispatch. For GitHub backend, infers milestone via infer_milestone_gh() when not provided; for fs backend uses MARKER_FILE. Issue state check confirms OPEN (GitHub) or ready/done status (fs) before printing runbook. Created tests/cmd_recover_spec.sh with 9 assertions covering runbook generation, missing baseline handling, and no-execution verification. All syntax checks clean. Updated .ralph.conf LINT_CMD to include new test file. Tests pass (pick_next_issue_spec.sh: 29/29, baseline_spec.sh: 8/8, recovery_plan_spec.sh: 12/12, cmd_status_spec.sh: 6/6, cmd_recover_spec.sh: 9/9).
issue-21-littlejs-recipe: Created littlejs standard recipe under skills/ralph-init/recipes/littlejs/: build.sh creates <repo>-littlejs sbx template with Node.js, Dockerfile specifies base image, README documents template usage. Updated SKILL.md monorepo flow: replaced TODO #21 with full littlejs standard tier documentation including steps to ensure vitest/eslint, copy bundled template files, verify shellcheck, emit config with TEST_CMD="npx vitest run" LINT_CMD="npx eslint ." and CLAUDE_CMD="sbx run <repo>-littlejs --", and optional build. Updated TODO section to mark #21 as authored. All acceptance criteria met. Tests pass (29/29 assertions).
23	issue-38-main-branch-guard: Implemented preflight guard in bin/ralph to refuse `ralph once` / `ralph run` when on main/master (or detected default branch), blocking WIP commits to main per ADR-0009. Added check_main_branch_guard() function that detects current/default branch via git, consults GitHub API if backend=github then falls back to local git, aborts with clear message telling operator to create/checkout ralph/<milestone> engagement branch. Guard overridable via RALPH_ALLOW_MAIN=1 env var for intentional main runs. Integrated into iterate_once() at entry point (returns exit code 11), updated run command to handle code 11 → exit 1. Added comprehensive test cases covering: on-main (abort), on-feature-branch (proceed), override (proceed), and message validation. All tests pass (33/33 assertions).
24	44	core config: select and validate an Agent CLI: Added core Agent CLI resolution in `bin/ralph` so engagements can declare `AGENT_CLI`, `AGENT_CMD`, `AGENT_ARGS`, `MODEL_CLASS`, and optional explicit `MODEL`, while preserving legacy `CLAUDE_CMD` as a Claude-only fallback with a once-per-run deprecation warning. `iterate_once()` now resolves and validates the adapter before work starts, prints manifest metadata for the selected Agent CLI and model/default source, maps `MODEL_CLASS=low` to `sonnet` for Claude and `gpt-5.4` for Codex, and leaves Pi model selection provider-default when no explicit model is set. Unsupported Agent CLI names and unsupported model classes now fail clearly before invocation, and sandbox name auto-derivation prefers `AGENT_CMD` while preserving legacy behavior. Expanded `tests/pick_next_issue_spec.sh` with coverage for new config resolution, legacy mapping precedence, explicit model overrides, Pi default-model omission, unsupported CLI rejection, and manifest formatting; kept `baseline`/`recover` specs shellcheck-clean. Post-push gate is green: `tests/pick_next_issue_spec.sh` 68/68, `baseline_spec.sh` 9/9, `recovery_plan_spec.sh` 12/12, `cmd_status_spec.sh` 6/6, `cmd_recover_spec.sh` 9/9, plus the required ShellCheck set.
25	44	agent cli config follow-up: Migrated the repo’s own `.ralph.conf` off deprecated `CLAUDE_CMD` onto `AGENT_CLI="claude"` plus `AGENT_CMD="/tmp/ralph-codex-claude-shim"`, extended `LINT_CMD` to include a dedicated `tests/agent_cli_config_spec.sh` regression suite, and fixed the lone post-push ShellCheck complaint in `bin/ralph`’s tty restore path. Verified again after the final push with the required gate (`bash tests/pick_next_issue_spec.sh` => 68/68, required ShellCheck set clean) and the focused adapter config spec (`agent_cli_config_spec.sh` => 37/37).
